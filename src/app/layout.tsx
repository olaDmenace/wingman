import type { Metadata } from "next";
import "./globals.css";

// Temporarily using system fonts due to Google Fonts connection issues during build
// Replace with Geist fonts when connection is stable
// import { Geist, Geist_Mono } from "next/font/google";
// const geistSans = Geist({
//   variable: "--font-geist-sans",
//   subsets: ["latin"],
// });
// const geistMono = Geist_Mono({
//   variable: "--font-geist-mono",
//   subsets: ["latin"],
// });

export const metadata: Metadata = {
  title: "Wingman Travel - Conversational Flight Search",
  description: "Find the best flights and track prices with our conversational AI assistant",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
